---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/app/page.tsx, src/components/layout/Header.tsx, src/components/layout/Hero.tsx, src/components/layout/SectionWrapper.tsx, src/components/layout/Footer.tsx]
autonomous: false
---

<objective>
Build responsive single-page scroll layout with all sections and deploy skeleton to Vercel.

Purpose: Create the page structure that Phase 2 fills with card content and Phase 3 animates. Verify the responsive layout and brand aesthetic work correctly before moving on.
Output: Working single-page layout (Header → Hero → Quick Wins → Medium-term → Strategic → Footer) deployed as skeleton to Vercel.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

@src/types/use-case.ts
@src/data/use-cases.ts
@src/app/globals.css
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build responsive single-page layout with section components</name>
  <files>src/app/page.tsx, src/components/layout/Header.tsx, src/components/layout/Hero.tsx, src/components/layout/SectionWrapper.tsx, src/components/layout/Footer.tsx</files>
  <action>
    1. Create `src/components/layout/Header.tsx`:
       - Fixed/sticky header with Allied logo (from public/allied-logo.svg) on the left
       - Clean, minimal header — navy background, logo in white/cream
       - Mobile: logo only, no hamburger needed (single-page scroll)
       - Use `next/image` for the SVG logo

    2. Create `src/components/layout/Hero.tsx`:
       - Full viewport height hero section with allied-navy background
       - Main heading: "Name the problem." (line break) "I build the solution." in large serif font, cream/white color
       - Subtitle: "10 AI-powered strategies to transform Allied Outdoor Solutions' sales, marketing, and operations" in sans-serif, slightly smaller
       - Generous whitespace, centered content, luxury feel
       - Responsive: smaller font sizes on mobile, maintained proportions
       - No animation yet — Phase 3 adds that. Just static positioning.
       - Include a subtle down-arrow or "Explore Solutions" indicator near the bottom

    3. Create `src/components/layout/SectionWrapper.tsx`:
       - Reusable section wrapper component
       - Props: `id: string`, `title: string`, `subtitle: string`, `category: TimelineCategory`, `children: ReactNode`
       - Alternating backgrounds: Quick Wins on cream, Medium-term on white, Strategic on cream (subtle visual separation)
       - Section heading in serif font, left-aligned or centered
       - Timeline badge (e.g., "1-2 Weeks") next to section title
       - Generous padding using the section-padding utility from globals.css
       - `max-w-7xl mx-auto` container for content

    4. Create `src/components/layout/Footer.tsx`:
       - CTA section: "Ready to transform Allied Outdoor Solutions?" heading
       - Contact prompt for reaching Gerhard (email placeholder, LinkedIn placeholder)
       - Allied logo small in footer
       - Navy background, cream text
       - Simple, clean — not cluttered

    5. Update `src/app/page.tsx` to compose the full single-page layout:
       - Header (sticky)
       - Hero section
       - SectionWrapper for "Quick Wins" (id="quick-wins") with placeholder text: "3 solutions coming in Phase 2"
       - SectionWrapper for "Medium-Term Plays" (id="medium-term") with placeholder: "4 solutions coming in Phase 2"
       - SectionWrapper for "Strategic Initiatives" (id="strategic") with placeholder: "3 solutions coming in Phase 2"
       - Footer with CTA
       - Import CATEGORY_SECTIONS from data to populate section titles

    The layout must be:
    - Mobile-first responsive (SITE-01)
    - Luxury aesthetic with earth tones and generous whitespace (SITE-03)
    - Single-page scroll structure (SITE-05)
    - Use the Allied design tokens from tailwind.config.ts — do NOT introduce new colors outside the palette
  </action>
  <verify>
    - `npm run build` succeeds
    - All 5 sections visible when scrolling (Hero, Quick Wins, Medium-term, Strategic, Footer)
    - Responsive: no horizontal scroll at 375px, 768px, 1024px, 1440px widths
  </verify>
  <done>Single-page layout renders with Header, Hero, 3 category sections, and Footer. Responsive at all breakpoints. Allied brand colors and typography used throughout. Placeholder content in sections ready for Phase 2 cards.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive single-page layout with Allied brand design system — Header, Hero ("Name the problem. I build the solution."), three category sections (Quick Wins, Medium-Term, Strategic) with placeholder content, and Footer CTA</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:3000
    3. Desktop (>1024px): Verify luxury aesthetic — navy hero, serif headings, cream sections, generous whitespace
    4. Mobile (375px): Verify no horizontal scroll, readable text, proper stacking
    5. Scroll: Verify all sections present — Hero → Quick Wins → Medium-Term → Strategic → Footer
    6. Check: Logo in header, footer CTA visible, overall "feels premium"
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Vercel deploy, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deploy skeleton to Vercel</name>
  <files>vercel.json</files>
  <action>
    1. Run `vercel --yes` to deploy the skeleton site
    2. Capture the deployment URL from output
    3. If authentication gate: stop and create checkpoint (user needs to run `vercel login`)

    Note: This is a skeleton deploy to verify the pipeline works. Final production deploy is Phase 5.
    Do NOT set up custom domain yet — that's Phase 5.
  </action>
  <verify>
    - `vercel ls` shows the deployment
    - `curl` the deployment URL returns 200
    - Site loads in browser at the Vercel URL
  </verify>
  <done>Site deployed to Vercel preview URL, accessible and rendering correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] All sections render on the page
- [ ] Responsive at mobile (375px), tablet (768px), desktop (1024px+)
- [ ] Human approved visual design
- [ ] Vercel deployment accessible
</verification>

<success_criteria>

- All tasks completed
- Human approved the visual layout
- Site deployed to Vercel
- Requirements satisfied: SITE-01 (responsive), SITE-03 (luxury aesthetic), SITE-05 (single-page scroll)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
