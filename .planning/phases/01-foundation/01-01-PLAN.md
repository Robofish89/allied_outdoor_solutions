---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, tailwind.config.ts, postcss.config.mjs, next.config.ts, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, src/types/use-case.ts, src/data/use-cases.ts, public/allied-logo.svg, .gitignore]
autonomous: true
---

<objective>
Scaffold Next.js project with Allied brand design system and typed data model.

Purpose: Create the foundation — project structure, brand-aligned design tokens, and the typed data model that all future phases build on.
Output: Working Next.js 15 app with Tailwind, Allied design tokens, typed use case data model with all 10 use cases stubbed, and basic page shell.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@allied_style_guide/Allied Logo.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with Tailwind and Allied design tokens</name>
  <files>package.json, tsconfig.json, tailwind.config.ts, postcss.config.mjs, next.config.ts, src/app/layout.tsx, src/app/globals.css, public/allied-logo.svg, .gitignore</files>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias --yes` in the project root to scaffold Next.js 15 with App Router and Tailwind.

    2. Copy `allied_style_guide/Allied Logo.svg` to `public/allied-logo.svg`.

    3. Configure `tailwind.config.ts` with Allied brand design tokens:
       - Colors: `allied-navy: '#0F2B46'` (primary dark navy from their branding), `allied-cream: '#F5F0EB'` (warm off-white background), `allied-gold: '#C4A35A'` (luxury accent), `allied-charcoal: '#2D2D2D'` (body text), `allied-slate: '#64748B'` (secondary text), `allied-white: '#FFFFFF'`
       - Font families: `serif: ['Georgia', 'Cambria', 'Times New Roman', 'serif']` for headings, `sans: ['system-ui', '-apple-system', 'Segoe UI', 'sans-serif']` for body. Use system fonts — no Google Fonts to keep loading fast.
       - Extend spacing/borderRadius as needed for generous whitespace feel.

    4. Set up `src/app/globals.css` with:
       - Tailwind directives (@tailwind base, components, utilities)
       - CSS custom properties mirroring the Allied color palette for easy reference
       - Base styles: `body` with `allied-cream` background, `allied-charcoal` text color, smooth font rendering (`antialiased`)
       - Heading styles using serif font family
       - A `.section-padding` utility for consistent vertical padding (py-20 md:py-28 lg:py-32)

    5. Set up `src/app/layout.tsx`:
       - HTML lang="en"
       - Metadata: title "Allied Outdoor Solutions — AI Strategy Showcase", description "10 AI-powered solutions to transform Allied Outdoor Solutions' sales, marketing, and operations"
       - Body with `min-h-screen` and the base font class

    6. Create a minimal `src/app/page.tsx` that renders a centered "Allied Outdoor Solutions — AI Strategy Showcase" heading using the serif font on the navy background — just enough to verify the design tokens work. This is a placeholder that Plan 02 will replace with the full layout.

    Do NOT install Framer Motion yet — that's Phase 3.
    Do NOT add any animation libraries.
    Keep it minimal — this is scaffolding, not the final design.
  </action>
  <verify>
    - `npm run build` succeeds without errors
    - `npm run dev` starts and page loads at localhost:3000
    - Page shows Allied navy background with serif heading in cream/white
  </verify>
  <done>Next.js 15 project scaffolded with Tailwind, Allied design tokens in tailwind.config.ts and globals.css, layout with metadata, placeholder page renders with correct brand colors and serif typography</done>
</task>

<task type="auto">
  <name>Task 2: Create typed use case data model with all 10 use cases</name>
  <files>src/types/use-case.ts, src/data/use-cases.ts</files>
  <action>
    1. Create `src/types/use-case.ts` with TypeScript interfaces:
       ```
       TimelineCategory = 'quick-win' | 'medium-term' | 'strategic'

       UseCase {
         id: string                    // e.g. 'rag-chatbot'
         title: string                 // e.g. 'RAG-Powered Website Chatbot'
         subtitle: string              // short tagline
         description: string           // 2-3 sentence explanation
         alliedContext: string          // Allied-specific angle (Carvestone, markets, competitors)
         tools: string[]               // ['OpenAI', 'LangChain', 'Pinecone']
         timeline: TimelineCategory    // 'quick-win' | 'medium-term' | 'strategic'
         timelineLabel: string         // '1-2 Weeks' | '1-3 Months' | '3-6 Months'
         roi: string                   // e.g. '24/7 availability, 3x consultation bookings'
         processSteps: ProcessStep[]   // for the animated visual in Phase 3
       }

       ProcessStep {
         label: string                 // e.g. 'Customer asks question'
         type: 'input' | 'ai' | 'output'
       }

       CategorySection {
         category: TimelineCategory
         label: string                 // 'Quick Wins' | 'Medium-Term Plays' | 'Strategic Initiatives'
         tagline: string               // section subtitle
       }
       ```

    2. Create `src/data/use-cases.ts` with all 10 use cases populated with real Allied-specific content:

       Quick Wins (1-2 Weeks):
       - UC-01: RAG Website Chatbot — answers Carvestone/service questions, books consultations across Austin/Dallas/Houston/Fort Worth/San Antonio
       - UC-05: Automated Review Solicitation — turns completed patio/kitchen/pool projects into 5-star Google reviews
       - UC-04: Sales Process Automation — automatic follow-up sequences after consultation requests

       Medium-Term (1-3 Months):
       - UC-02: Internal Sales Assistant RAG — Carvestone specs, pergola options, pricing tiers for new reps
       - UC-06: Contact List Reengagement — research dormant CRM leads, find homeowners who moved or got new pools
       - UC-07: Lead Scoring & CRM Enrichment — identify high-value leads (pool + outdoor kitchen combo vs. simple patio)
       - UC-08: Competitor Social & Ad Research — analyze what Belgard, Tremron, Tex Sun are promoting

       Strategic (3-6 Months):
       - UC-03: AI Project Status Alerts — automatic delay detection across 5 markets, client communication triggers
       - UC-09: Lead Gen via Prospect Scraping — find luxury homeowners in Highland Park, River Oaks, Westlake with outdated outdoor spaces
       - UC-10: AI Visual Project Estimator "Backyard Vision" — homeowner uploads photo, AI generates Carvestone/pergola overlay + rough estimate

       Each use case must have 3 processSteps (input → AI → output pattern).
       Include a `CATEGORY_SECTIONS` export with the 3 category definitions.
       Include a `USE_CASES` export as the array of all 10.

       Content must be specific to Allied — reference Carvestone by name, mention their 5 Texas markets, reference real competitor types, reference their actual services (patios, pergolas, outdoor kitchens, fire features, pools, landscaping).
  </action>
  <verify>
    - `npx tsc --noEmit` passes (no TypeScript errors)
    - All 10 use cases present in the array
    - Each use case has all required fields populated (no empty strings)
    - Categories: 3 quick-win, 4 medium-term, 3 strategic
  </verify>
  <done>TypeScript interfaces exported from src/types/use-case.ts, all 10 use cases with Allied-specific content in src/data/use-cases.ts, zero TypeScript errors, all fields populated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npx tsc --noEmit` passes
- [ ] Dev server shows page with Allied navy + serif typography
- [ ] All 10 use cases in data file with no empty fields
- [ ] Allied logo SVG copied to public/
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Next.js project builds cleanly
- Design tokens match Allied brand (navy, cream, serif headings)
- Data model is typed and complete with all 10 use cases
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
