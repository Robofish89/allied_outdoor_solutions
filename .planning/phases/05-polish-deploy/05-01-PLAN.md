---
phase: 05-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/layout.tsx, public/og-image.png]
autonomous: false
---

<objective>
Add OG meta tags for social sharing, verify performance, and deploy final production build to Vercel.

Purpose: Make the site production-ready — shareable with a rich preview card, fast-loading, and live on Vercel.
Output: Deployed production site with OG tags, verified under 2-second load time.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/layout.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OpenGraph and Twitter meta tags for social sharing</name>
  <files>src/app/layout.tsx</files>
  <action>
Update the Next.js Metadata export in layout.tsx to include comprehensive OG and Twitter card tags:

- `openGraph.title`: "Allied Outdoor Solutions — AI Strategy Showcase"
- `openGraph.description`: "10 AI-powered solutions to transform Allied Outdoor Solutions' sales, marketing, and operations across Austin, Dallas, Fort Worth, Houston, and San Antonio."
- `openGraph.type`: "website"
- `openGraph.url`: "https://alliedoutdoorsolutions.vercel.app"
- `openGraph.siteName`: "Allied Outdoor Solutions AI Showcase"
- `openGraph.images`: Use a 1200x630 OG image. Generate a simple branded OG image using the `ImageResponse` approach from `next/og` by creating a `src/app/opengraph-image.tsx` file (Next.js auto-routes this). The image should have the Allied earth-tone background (#2C2C2C dark or #F5F0EB cream), "Allied Outdoor Solutions" in serif font, "10 AI-Powered Solutions" subtitle, and "by Gerhard van den Heever" at bottom. Use Next.js built-in OG image generation — do NOT create a static PNG.
- `twitter.card`: "summary_large_image"
- `twitter.title`: same as OG title
- `twitter.description`: same as OG description

Use Next.js 16 Metadata API (the existing export pattern in layout.tsx). Do NOT install any additional packages.
  </action>
  <verify>npm run build succeeds without errors; check that the OG image route is generated</verify>
  <done>layout.tsx exports full OG + Twitter metadata; opengraph-image.tsx exists and generates branded image on build</done>
</task>

<task type="auto">
  <name>Task 2: Verify performance and deploy production build to Vercel</name>
  <files>vercel deployment</files>
  <action>
1. Run `npm run build` and check the output for:
   - No build errors or warnings
   - Route sizes — flag any route over 200KB as concern
   - Total First Load JS shared — should be reasonable for Next.js 16

2. Deploy to Vercel production:
   - Run `vercel --prod` to deploy production build
   - Capture the production URL from output

3. Verify the deployed site:
   - `curl -s -o /dev/null -w "%{time_total}" https://alliedoutdoorsolutions.vercel.app` — should be under 2 seconds
   - `curl -s -I https://alliedoutdoorsolutions.vercel.app` — check for 200 status and proper content-type

Performance is validated by build output (bundle sizes) and curl response time. System fonts + no heavy JS dependencies keep this lean.
  </action>
  <verify>vercel --prod succeeds; curl response time under 2s; HTTP 200 returned</verify>
  <done>Production deployment live on Vercel, response time under 2 seconds, build has no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Production site deployed with OG meta tags, performance verified</what-built>
  <how-to-verify>
    1. Visit: https://alliedoutdoorsolutions.vercel.app
    2. Mobile: Open on phone or use Chrome DevTools mobile emulation (375px width)
       - Hero section readable, no horizontal scroll
       - Cards stack properly in single column
       - Footer CTA visible and tappable
       - PDF download button works
    3. Social sharing preview: Paste the URL into https://www.opengraph.xyz/ or Twitter/LinkedIn compose
       - Should show branded preview card with title, description, and OG image
    4. PDF: Click download button — PDF generates and downloads correctly
  </how-to-verify>
  <resume-signal>Type "approved" to finalize, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] OG meta tags present in page source
- [ ] OG image route generates branded image
- [ ] Production deployment live on Vercel
- [ ] Response time under 2 seconds
- [ ] Mobile layout verified by user
- [ ] Social sharing preview verified by user
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OG tags render proper social sharing preview
- Site loads under 2 seconds on Vercel
- Mobile layout works correctly
- PDF download works on production deployment
  </success_criteria>

<output>
After completion, create `.planning/phases/05-polish-deploy/05-01-SUMMARY.md`
</output>
